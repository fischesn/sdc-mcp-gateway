schema_version: "1"
study_id: "anonymous-bhi2026-revision"
provider_identity: "sim-ventilator-1"
gateway_config: "config/gateway.simulated.dryrun.high-airway-pressure.example.yaml"
mapping: "config/sdc_mie.yaml"
policy: "config/tool_policies.yaml"
task: "Validate provenance and two non-executing FiO2 proposals against one frozen snapshot."
prompt: "Use only the supplied resource snapshot; report the deterministic policy decision."
model_id: "deterministic-policy-engine-v0.13.0"
output_dir: "data/revision/security/bhi2026-wp9-security-v1"
